@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>User Settings and Actions</h2>

<div>
    <div class="col-xs-4">
       @{ Html.RenderPartial("PasswordReset"); }
    </div>
    <div class="col-xs-4">
        @{ Html.RenderPartial("ReplaceProfile"); }
    </div>
    <div class="col-xs-4">
        @{ Html.RenderPartial("DeleteProfile"); }
    </div>
</div>